import {useEffect,useState} from 'react';import api from '../api/client';import AppShell from '../layouts/AppShell';
export default function ServicesPage(){const [rows,setRows]=useState<any[]>([]),[f,setF]=useState({name:'',category:'',description:''});const load=()=>api.get('/mis/services').then(r=>setRows(r.data));useEffect(()=>{load()},[]);const save=async(e:any)=>{e.preventDefault();await api.post('/mis/services',f);setF({name:'',category:'',description:''});load()};return <AppShell title="Services" intro="Administrators can add services such as counseling, job readiness, resume writing, and movie night."><form className="panel form-grid" onSubmit={save}><label>Name<input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Category<input value={f.category} onChange={e=>setF({...f,category:e.target.value})}/></label><label className="full-span">Description<textarea value={f.description} onChange={e=>setF({...f,description:e.target.value})}/></label><div className="full-span"><button>Add service</button></div></form><section className="panel">{rows.map(s=><div className="detail-banner" key={s.id}><div><strong>{s.name}</strong><span>{s.category||'General'}</span></div></div>)}</section></AppShell>}
