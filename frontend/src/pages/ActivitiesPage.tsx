import {useEffect,useState} from 'react';import api from '../api/client';import AppShell from '../layouts/AppShell';
export default function ActivitiesPage(){const [rows,setRows]=useState<any[]>([]),[f,setF]=useState({title:'',activity_date:'',start_time:'',end_time:'',location:'',description:''});const load=()=>api.get('/mis/activities').then(r=>setRows(r.data));useEffect(()=>{load()},[]);const save=async(e:any)=>{e.preventDefault();await api.post('/mis/activities',f);setF({...f,title:'',description:''});load()};return <AppShell title="Activities Calendar" intro="Schedule events, activities, workshops, and appointments."><form className="panel form-grid" onSubmit={save}><label>Activity<input required value={f.title} onChange={e=>setF({...f,title:e.target.value})}/></label><label>Date<input required type="date" value={f.activity_date} onChange={e=>setF({...f,activity_date:e.target.value})}/></label><label>Start<input type="time" value={f.start_time} onChange={e=>setF({...f,start_time:e.target.value})}/></label><label>End<input type="time" value={f.end_time} onChange={e=>setF({...f,end_time:e.target.value})}/></label><label>Location<input value={f.location} onChange={e=>setF({...f,location:e.target.value})}/></label><label className="full-span">Description<textarea value={f.description} onChange={e=>setF({...f,description:e.target.value})}/></label><div className="full-span"><button>Schedule activity</button></div></form><section className="panel">{rows.map(a=><div className="detail-banner" key={a.id}><div><span>{a.activity_date} {a.start_time||''}</span><strong>{a.title}</strong><small>{a.location}</small></div></div>)}</section></AppShell>}
